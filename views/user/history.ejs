<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Links - MiniLink</title>
    <link
        href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
        rel="stylesheet"
    />
    <style>
        @import url('https://fonts.cdnfonts.com/css/playpen-sans');

        /* Custom styling */
        body {
            background-color: black;
            font-family: 'Playpen Sans', sans-serif;
        }

        /* Table styling */
        table {
            width: 100%;
            max-width: 900px;
            margin: auto;
            border-collapse: collapse;
        }

        th, td {
            border: 1px solid #3b82f6; /* blue border */
            padding: 1rem;
            color: white;
            text-align: left;
        }

        th {
            font-weight: bold;
        }

        td a {
            color: #3b82f6;
            text-decoration: underline;
            word-break: break-all;
        }

        .delete-icon {
            cursor: pointer;
            color: white;
        }

        .delete-icon:hover {
            color: red;
        }
    </style>
</head>
<body class="min-h-screen flex flex-col">
    <!-- Navbar -->
    <nav class="p-6 bg-black flex justify-between items-center text-white">
        <a href="/" class="text-2xl font-bold">MiniLink</a>
        <div>
            <a href="/" class="text-white mr-6">Home</a>
            <a href="/history" class="text-white mr-6">History</a>
            <a href="/profile" class="text-white">Profile</a>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container mx-auto flex-grow flex justify-center items-center">
        <div class="w-full max-w-4xl text-center">
            <h1 class="text-3xl font-bold text-white mb-6">Your Links</h1>
            <table class="border-separate border-spacing-y-2">
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Original Link</th>
                        <th>Shortened Link</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% links.forEach(function(link) { %>
                        <tr>
                            <td><%= //link.createdAt %>
                            <%= new Date(link.createdAt).toLocaleDateString('en-GB', { month: 'short', day: 'numeric', year: 'numeric' }) %>
                            </td>
                            <td>
                                <a href="<%= link.url %>" target="_blank">
                                    <%= link.url %>
                                </a>
                            </td>
                            <td>
                                <a href="http://localhost:3000/u/<%= link.key %>" target="_blank">
                                    http://localhost:3000/u/<%= link.key %>
                                </a>
                            </td>
                            <td>
                                <form action="/history/<%= link.id %>" method="POST" class="inline-block">
                                    <button type="submit" class="delete-icon">
                                        &#128465;
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </main>
</body>
</html>
