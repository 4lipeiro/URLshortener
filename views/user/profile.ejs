<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Profile - MiniLink</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
    <style>
        @import url('https://fonts.cdnfonts.com/css/playpen-sans');

        /* Custom styling */
        body {
            background-color: black;
            font-family: 'Playpen Sans', sans-serif;
        }

        /* Centering the content */
        .main-content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

        /* Form fields styling */
        input {
            background-color: transparent;
            border: 2px solid #3b82f6;
            /* blue */
            color: white;
            padding: 0.5rem;
            width: 100%;
            margin-bottom: 1rem;
            border-radius: 0.375rem;
        }

        /* Button styling */
        button {
            background-color: #3b82f6;
            /* blue */
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 0.375rem;
            width: 100%;
            cursor: pointer;
        }

        button:hover {
            background-color: #2563eb;
            /* darken blue */
        }
    </style>
</head>

<body class="min-h-screen flex flex-col">
    <!-- Navbar -->
    <%- include('navbar') %>

        <!-- Main Content -->
        <main class="container mx-auto flex-grow flex justify-center items-center">
            <div class="main-content">
                <h1 class="text-3xl font-bold text-white mb-6">Edit Profile</h1>
                <form action="/profile" method="POST" class="w-full">
                    <input type="text" name="username" placeholder="Username" value="<%= username %>"
                        placeholder="Username"
                        class="appearance-none bg-transparent w-full text-white py-2 px-4 leading-tight rounded focus:outline-none" />
                    <input type="email" name="email" placeholder="Email" value="<%= email %>" placeholder="Email"
                        class="appearance-none bg-transparent w-full text-white py-2 px-4 leading-tight rounded focus:outline-none" />
                    <input type="password" name="password" placeholder="New Password (optional)"
                        class="appearance-none bg-transparent w-full text-white py-2 px-4 leading-tight rounded focus:outline-none" />
                    <input type="password" name="confirm_password" placeholder="Confirm New Password"
                        class="appearance-none bg-transparent w-full text-white py-2 px-4 leading-tight rounded focus:outline-none" />
                    <button type="submit"
                        class="bg-blue-500 hover:bg-blue-700 text-white py-2 px-6 rounded focus:outline-none w-full">
                        Save Changes
                    </button>
                </form>
            </div>
        </main>
</body>

</html>