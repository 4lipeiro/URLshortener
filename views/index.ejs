<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MiniLink - URL Shortener</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
    <style>
        @import url('https://fonts.cdnfonts.com/css/playpen-sans');

        /* Custom styling */
        body {
            background-color: black;
            font-family: 'Playpen Sans', sans-serif;
        }

        /* Centering the main content */
        .main-content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 100%;
            max-width: 500px;
            text-align: center;
        }
    </style>
</head>

<body class="min-h-screen flex flex-col">
    <!-- Navbar -->
    <%- include('user/navbar') %>

        <!-- Main Content -->
        <main class="container mx-auto flex-grow flex justify-center items-center">
            <div class="main-content">
                <h1 class="text-3xl font-bold text-white mb-4">Create a Short URL</h1>
                <form action="/" method="POST" class="w-full">
                    <div class="mb-4">
                        <input type="url" name="original_url" placeholder="Enter URL here"
                            class="appearance-none bg-transparent border-2 border-blue-500 w-full text-white py-2 px-4 leading-tight rounded focus:outline-none"
                            required />
                    </div>
                    <button type="submit"
                        class="bg-blue-500 hover:bg-blue-700 text-white py-2 px-6 rounded focus:outline-none w-full mb-4">
                        Shorten
                    </button>
                </form>

                <% if (shortUrl) { %>
                    <h3 class="text-white">Your Shortened URL:</h3>
                    <p>
                        <a href="<%= shortUrl %>" target="_blank" class="text-blue-400 hover:text-blue-600">
                            <%= shortUrl %>
                        </a>
                    </p>
                    <% } %>
            </div>
        </main>
</body>

</html>